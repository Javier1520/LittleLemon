restaurant                       --->    (displays the static index.html)
restaurant/menu
restaurant/menu/{menuId}
restaurant/booking/tables
restaurant/booking/tables/{tableId}
auth/token/login/                        (auth)
auth/users/                              (registration)


Dont forget to install the requirements.txt
pip install -r requirements.txt